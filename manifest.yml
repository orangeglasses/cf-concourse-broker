---
buildpack: go_buildpack
applications:
- name: "cf-concourse-broker"
  command: cf-concourse-broker
  memory: 512M
  env:
  # BROKER_USERNAME:
  # BROKER_PASSWORD:
  # ADMIN_USERNAME:
  # ADMIN_PASSWORD:
  # CONCOURSE_URL:
  # CF_URL:
  # AUTH_URL:
  # TOKEN_URL:
  # CLIENT_ID:
  # CLIENT_SECRET:
